# =============================================================================
# 容器內的 apps/api/.env——隊友共用的容器基準
# -----------------------------------------------------------------------------
# compose.yml 的 x-app-base 把本檔掛到容器的 apps/api/.env，遮蔽掉 host 的同名檔。
#
# 但**容器並不是讀不到 host 的設定**：compose 的 api 服務另外用
# `env_file: ./apps/api/.env`（required: false）刻意讀它。優先序是
#
#   compose 的 environment  >  env_file（host 的 .env）  >  本檔  >  envSchema 預設
#
# 所以這個掛載不是「防止本機設定進來」的手段——真正防止連線類被打壞的是
# compose 的 environment 把 REDIS_URL / SMTP_HOST / SMTP_PORT / API_BASE_URL 釘死
# （優先序最高，host 蓋不掉），並有守則擋住新增時漏釘。
#
# 保留掛載是為了讓兩個檔各有職責：
#
#   本檔（進版控）        → 隊友共用的容器基準，所有人跑起來一樣
#   apps/api/.env（不進版控）→ 個人偏好，只影響自己這台
#
# 也就是說：**要全隊生效的容器設定寫這裡，只想自己生效的寫 apps/api/.env。**
# ⚠️ 注意優先序——本檔的值會被 host 的 .env 蓋掉，需要不可被覆寫的請寫進
# compose 的 environment。
#
# 改完 `pnpm docker:up` 重啟即可（本檔是 bind mount，不必重建映像）。
# =============================================================================
